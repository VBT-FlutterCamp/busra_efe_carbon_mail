import 'package:flutter/material.dart';

class AppColor {
  final Color textColor = Color.fromARGB(255, 7, 45, 77);
  final Color buttonColor = Colors.white;
  final Color homeBlue = Color.fromARGB(255, 25, 87, 138);
  final Color keepTextColor = Colors.white;
  final Color cleanBack = Colors.blueGrey.withOpacity(0.5);
}
